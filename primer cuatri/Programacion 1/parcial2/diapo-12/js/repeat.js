'use strict';

// String:
let cadena = 'no ';

// Aplicación del método:
let cedenaLarga = cadena.repeat(5);

// Resultados:
console.info('cedenaLarga:', cedenaLarga);

let html = `<ul>`;
html += (`<li>Item</li>`).repeat(5);
html += `</ul>`;

