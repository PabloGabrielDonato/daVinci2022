'use strict';

// Declaración de variables:
let m,
	inf,
	sup,
	num;

// Ingreso de datos:
inf = parseInt(prompt('Ingrese el extremo inferior'));
sup = parseInt(prompt('Ingrese el extremo superior'));
num = parseInt(prompt('Ingrese el número'));