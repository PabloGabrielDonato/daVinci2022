'use strict';

// Declaración de variables:
let m,
    cat,
    sub,
    sueldo;

// Ingreso de datos:
cat = parseInt(prompt('Ingrese la categoría a la que pertenece (1, 2, 3)'));
sub = prompt('Ingrese la subcategoría a la que pertenece (A, B, C)');