/*'use strict';

// Inicio m para que no se concatenen los mensajes:
m = '';

// Inicio i:
i = 0;

// while:
while (i < 3) {
    // Ingreso de datos:
    n = parseInt(prompt('Ingrese una nota para el while'));
    // Consola:
    console.info('Valor de i:', i, 'Nota ingresada por while:', n);
    // Incremento del "contador":
    i++;
    // Armo el mensaje:
    m += `<li>Nota ${i}: <strong>${n}</strong>.</li>`;
}*/
