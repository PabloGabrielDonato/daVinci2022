/*'use strict';

// Reinicio m para que no se concatenen los mensajes:
m = '';

// Reinicio i:
i = 0;

// do-while:
do {
    // Ingreso de datos:
    n = parseInt(prompt('Ingrese una nota para el do-while'));
    // Consola:
    console.info('Valor de i:', i, 'Nota ingresada por do-while:', n);
    // Armo el mensaje:
    m += `<li>Nota ${++i}: <strong>${n}</strong>.</li>`;
    // Esa ++i es un pre incremento, es decir, va a incrementar el valor de i para luego mostrarlo aumentado.
} while (confirm('¿Desea ingresar una nota más?'))*/